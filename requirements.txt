# Core (필수, librosa 폴백 경로만으로도 전체 파이프라인 작동)
numpy>=1.22,<2.0
scipy>=1.9
librosa>=0.10
soundfile>=0.12
mido>=1.3
matplotlib>=3.6
pandas>=1.5
tqdm>=4.65
sounddevice>=0.5     # for align_viewer.py audio playback

# Stage 1: Demucs 드럼 분리 (필수 수준, 무겁지만 설치 안정적)
# CPU/MPS만 써도 동작. M1/M2 Mac이면 MPS 가속.
demucs>=4.0
torch>=2.0
torchaudio>=2.0

# Stage 4: madmom 비트 트래커 (강력 추천, 실패시 librosa.beat 폴백)
# Python 3.9에서는 보통 잘 깔림. 3.11+에서는 Cython 충돌 있을 수 있음.
# 설치 실패해도 파이프라인은 librosa 폴백으로 계속 동작.
madmom>=0.16.1

# Stage 2: ADTOF (선택사항, README의 별도 설치 항목 참고)
# pip로는 안 깔리고 git clone 후 별도 설치 필요.
# 미설치시 librosa 대역별 폴백 사용.
